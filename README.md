# PROJECT-MA Mockups

This orphan branch stores design mockups only.

- Branch: `codex/mockups`
- Implementation source of truth: `develop` or the feature branch named by each mockup
- Do not treat files on this branch as a code snapshot.
- Do not add Source, Content, Config, Data, Plugins, or project binaries here.
- New mockup topics belong under `docs/wip/<topic>/`.
- This branch is not merged wholesale into implementation branches. Confirmed decisions are transferred deliberately.
